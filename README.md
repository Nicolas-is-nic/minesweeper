# minesweeper
minesweeper

#### 打包命令：

mac版：

```shell
pyinstaller --onefile --windowed --icon=icons/favicon.ico --add-data "icons:icons" minesweeper.py
```

win版：


```shell
pyinstaller --onefile --windowed --icon=icons/favicon.ico --add-data "icons;icons" minesweeper.py
```
